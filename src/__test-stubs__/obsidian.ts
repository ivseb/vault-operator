/**
 * Minimal stub for the `obsidian` module — only covers what test files import.
 * All Obsidian Plugin-runtime imports in production code are `import type`,
 * so nothing is actually called at runtime during tests.
 */
export class Vault {}
export class App {}
export class Plugin {}
export class TFile {}
export class TFolder {}
