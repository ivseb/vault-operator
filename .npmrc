# Pin this project to the public npm registry.
# The global ~/.npmrc may point npm at a private company mirror, which
# is only wanted in a separate internal project. Pinning here keeps that
# host (and its resolved URLs) out of this repo's package-lock.json and out
# of the public mirror + CI builds.
registry=https://registry.npmjs.org/
omit=optional
