# Quality Rules -- __PROJECT_NAME__

> Framework-spezifische Regeln die der Linter nicht abfaengt.
> Diese Datei wird ab der ersten Implementierung befuellt.
> Verlinkt aus MEMORY.md unter "Coding Rules".

---

## Regeln

[Hier framework-spezifische Regeln eintragen, z.B.:]

<!--
Beispiele:

### Obsidian Plugin
- Kein `console.log()` -- nutze `console.debug()`, `.warn()`, `.error()`
- Kein `fetch()` -- nutze `requestUrl` aus obsidian
- Kein `innerHTML` -- nutze Obsidian DOM API (`createEl`, `createDiv`)
- Kein `require()` -- nutze ES `import`

### React App
- Kein `dangerouslySetInnerHTML` ohne Sanitizer
- Kein direkter DOM-Zugriff -- nutze Refs
- Kein `useEffect` ohne Dependency-Array

### Node.js Backend
- Kein `eval()` oder `new Function()`
- Kein `child_process.exec()` mit User-Input
- Kein synchrones I/O in Request-Handlern
-->
